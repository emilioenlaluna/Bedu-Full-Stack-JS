git config --global user.name "emilio"

git config --global user.email "emilioenlaluna@dj.com"

git config --global -l


git init

git add .

git commit -m "first commit"

git branch -M main

echo "# Bedu-Full-Stack-JS" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/emilioenlaluna/Bedu-Full-Stack-JS.git
git push -u origin main

git status

git branch

git flow

git branch testing


git status

git branch testing


git checkout testing //cambiar de rama

git branch//ver ramas



hacer merge de ramas

//cambiar a la rama
git checkout main
git merge testing